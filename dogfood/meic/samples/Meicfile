#! /usr/bin/env ekam

comp = "tigc"
target = "app"
flags = "-O2 -Wall"
src  = "main.tc"   
install_dir = "/usr/local/bin"

build:
    {comp} {src} -c {target} -- {flags}

install:
    cp {target} {install_dir}

clean:
    rm -f *.o 

run:
    @build # calls the build cmd from above
    ./{target}