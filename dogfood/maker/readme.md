# Maker
Maker is just Make rebuilt for and with Tig and with more intuitive syntax.

# Features
- More intuitive syntax
- Better error messages
- Incremental builds
- Uses `Maikfile` instead of `Makefile`
