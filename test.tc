use "stdlib/io.tc"
use "stdlib/async.tc"

async fn void foo: i32 x, queue<i32> q{
    q.push(x*x)
}

fn void main: {
    queue<i32> q = {};

    foo(3, q)
    i32 y = q.pop();
    printi(y) // should print 9
}