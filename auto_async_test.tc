use "stdlib/async.tc"
use "stdlib/io.tc"

async fn void worker: i32 x {
    printi(x)
}

fn void main: {
    // No manual async_init() or async_shutdown() needed!
    // Runtime is automatically initialized on first async call
    
    printi(1)
    worker(42)
    printi(2)
}
