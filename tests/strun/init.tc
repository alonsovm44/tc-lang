use "stdlib/io.tc"

strun Point {
    i32 x,
    i32 y
}

fn void init: ->Point p, i32 a, i32 b {
    p.>x = a
    p.>y = b
}
fn i32 main: {
    Point p
    init(&p, 10, 20)
    printf("[%d, %d]\n", p.x, p.y)
    ret 0
}