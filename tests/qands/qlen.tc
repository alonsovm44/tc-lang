use "stdlib/io.tc"
use "stdlib/snq.tc" // NEW: module for stacks and queues

fn i32 main:
{
    queue<i32> q = {0,1,2,3} 

    printi(sizeof(q)) // should print 16 // prints 32?? i32 is 4 bytes, plus the end \0 , but it prints double that?
    
}