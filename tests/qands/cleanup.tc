use "stdlib/io.tc"
use "stdlib/snq.tc"

fn i32 main: {
    queue<i32> q = {1, 2, 3}
    print("Queue size: ")
    printi(q.size())
    print("\n")
    // Queue should be automatically destroyed when it goes out of scope
    ret 0
}
