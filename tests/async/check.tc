use "stdlib/io.tc"

// checking if declaring queues/stacks invoke the async runtime automatically (which is wrong)
// OK() it doesn not.
fn void main: 
{
    queue<i32> q
    stack<i32> s
    print("Hello, World!")

}